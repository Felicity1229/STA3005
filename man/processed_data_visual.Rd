% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processed_data_visual.R
\name{processed_data_visual}
\alias{processed_data_visual}
\title{Visualize Dataset with Binary Target Variable}
\usage{
processed_data_visual(
  df,
  target_col = "Potability",
  class_labels = NULL,
  feature_cols = NULL,
  correlation_cols = NULL
)
}
\arguments{
\item{df}{A data frame containing features and a binary target column.}

\item{target_col}{Name of the binary target column (default: "Potability").
The target column should be binary (0 = negative class, 1 = positive class).}

\item{class_labels}{Optional character vector of length 2 for class labels.
If NULL, uses c("Class 0", "Class 1") or detects from data.}

\item{feature_cols}{Optional character vector of feature column names.
If NULL, all columns except target_col will be used as features.}

\item{correlation_cols}{Optional integer vector or character vector for correlation plot.
If NULL, uses first 9 features or all numeric features.}
}
\value{
NULL. The function prints plots to the graphics device and returns nothing invisibly.
}
\description{
This function generates a comprehensive set of visualizations for dataset analysis,
including pie chart, boxplots, histograms, and correlation matrix.
}
\examples{
\dontrun{
# Default usage (assumes "Potability" column exists)
processed_data_visual(df)

# Custom target column
processed_data_visual(df, target_col = "Status")

# Custom target column with labels
processed_data_visual(df, target_col = "Success",
                      class_labels = c("Failure", "Success"))

# Specify which columns to visualize
processed_data_visual(df, target_col = "Potability",
                      feature_cols = c("pH", "Hardness", "Solids"))
}

}
\seealso{
\code{\link{Exploratory_Data_analysis}} for the EDA function
}
