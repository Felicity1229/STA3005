% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RandomForests.R
\name{rf_build_tree}
\alias{rf_build_tree}
\title{Build Decision Tree}
\usage{
rf_build_tree(X, y, depth = 0, max_depth = 5, mtry = NULL)
}
\arguments{
\item{X}{Feature data frame. Each column is a feature, each row is a sample.}

\item{y}{Vector of class labels (0/1).}

\item{depth}{Current depth in the tree (starts at 0).}

\item{max_depth}{Maximum depth of the tree.}

\item{mtry}{Number of features to consider at each split. Default is
floor(sqrt(ncol(X))).}
}
\value{
A list representing a decision tree node:
\item{label}{If leaf node, the predicted class}
\item{feature}{If internal node, the splitting feature name}
\item{value}{If internal node, the splitting threshold}
\item{left}{Left child subtree}
\item{right}{Right child subtree}
}
\description{
Recursively builds a binary decision tree using Gini impurity as the
splitting criterion. Stopping occurs when either max_depth is reached
or the node becomes pure.
}
